func calculateArea(length: Double, width: Double) -> Double {
    return length * width
}

let area = calculateArea(length: 10, width: 5) // Correct usage

let correctArea = calculateArea(length: 10, width: 5) // Correct usage with parameter labels